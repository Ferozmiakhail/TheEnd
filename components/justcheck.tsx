// "use client"
// import React, { useState, useEffect } from 'react';
// import { Product, Navbar, Herobanner, Footerbanner } from "@/components/index";
// import { getproduct } from '@/sanity/actions';
// import Image from 'next/image';

// import { useClient } from '@/lib/useClient'

// export const revalidate = 900;

// const Page = () => {
//   const categorys: { name: string, type: string }[] = [
//   { name: "Male", type: "Clothes" },
//   { name: "Female", type: "Clothes" },
//   { name: "Femalefirst", type: "Clothes" },
//   { name: "Jewellery", type: "Clothes" },
//   { name: "Cotton", type: "Others" },
//   { name: "Suit", type: "Clothes" },
//   { name: "MaleT", type: "Clothes" },
//   { name: "MaleG", type: "Clothes" },
//   { name: "Headboard", type: "Others" },
//   { name: "Cushion", type: "Others" },
//   { name: "Femalesecond", type: "Clothes" },
//   { name: "Femalefourth", type: "Clothes" }
// ];
  // const [Buyinggudidelist, setBuyinggudidelist] = useState(false);
    // const [tabType, settabType] = useState("Clothes")
    // const toggleMe=(value: string)=>{
    //   settabType(value)
    // }
    // const clickMe=(value: string)=>{
    //   setCType(value)
    // }

  // const product = await getproduct();

  // const [showGuide, setShowGuide] = useState(false);
  //  useEffect(() => {
  //   // Fetch product data asynchronously
  //   const fetchData = async () => {
  //     try {
  //       const productData = await getproduct();
  //       // Update state with fetched data
  //       // Example: setCategorys(productData);
  //     } catch (error) {
  //       console.error('Error fetching product data:', error);
  //     }
  //   };

  //   fetchData();
  // }, []);
  // return (
    // <>
{/*       <Navbar /> */}
{/*       <Herobanner /> */}

      // {showGuide ? <div>Show</div> : <div>No!</div>}
      // <section className="flex flex-wrap justify-center gap-[15px] mt-[20px] width-full">
{/*       salam mama hale saresh kar karda merom ta shab pas chez ha ra dobara mearom */}
{/*         {CType === "Male" && product.map((item: any) => (item.productcategory === "Male" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Female" && product.map((item: any) => (item.productcategory === "Female" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Female" && product.map((item: any) => (item.productcategory === "Femalefirst" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Cotton" && product.map((item: any) => (item.productcategory === "Cotton" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Male" && product.map((item: any) => (item.productcategory === "Suit" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Male" && product.map((item: any) => (item.productcategory === "MaleT" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Male" && product.map((item: any) => (item.productcategory === "MaleG" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Headboard" && product.map((item: any) => (item.productcategory === "Headboard" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Headboard" && product.map((item: any) => (item.productcategory === "Cotton" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Headboard" && product.map((item: any) => (item.productcategory === "Cushion" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Cushion" && product.map((item: any) => (item.productcategory === "Cushion" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Female" && product.map((item: any) => (item.productcategory === "Femalesecond" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
        {CType === "Female" && product.map((item: any) => (item.productcategory === "Femalefourth" && <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))} */}


{/*         {product.map((item: any) => (
          (
            <Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />
          )
        ))} */}
{/*         {product.map((item: any) => (<Product key={item._id} image={item.image} name={item.name} Sdetails={item.Sdetails} price={item.price} />))}
      </section> */}

      // <Footerbanner />
    // </>
//   )
// }

// export default Page
